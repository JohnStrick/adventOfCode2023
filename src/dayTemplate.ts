const testData: string[] = [

];

type Puzzle = {

}

function parse(data: string[]): Puzzle {
    let puzzle = {};
    return puzzle;
}

function part1(data: string[] = testData): number {
    return -1;
}

function part2(data: string[] = testData): number {
    return -1;
}

function run(data: string[]): number {
    return part1();
    // return part1(data); 
    // return part2();
    // return part2(data);
}

export default run;